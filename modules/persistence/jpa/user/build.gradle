bootJar {
    enabled = false
}

jar {
    enabled = true
}

dependencies {
    api project(':modules:persistence:adapter:common')
    api project(':modules:utils:generator')

    // for Test
    runtimeOnly 'com.h2database:h2'
    testImplementation 'org.mockito:mockito-inline:3.6.0'
}